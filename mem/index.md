# Project Memory

## Core
Todos los botones de guardado son REDONDOS y VERDES (emerald-600). El botón principal de cada página va FIJO en el extremo inferior derecho. Usar siempre `<SaveButton>` de `@/components/save-button`; `floating` para el botón principal de la página, sin `floating` (inline, size sm) para los sub-editores.
